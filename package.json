{
  "name": "concurrent-map-stream",
  "description": "Concurrently transform a stream in FIFO order",
  "keywords": [
    "stream",
    "concurrent",
    "map"
  ],
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/jb55/concurrent-map-stream.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec"
  },
  "dependencies": {
    "SimpleQueue": "0.0.1",
    "debug": "^0.8.0",
    "duplex": "^1.0.0"
  },
  "devDependencies": {
    "mocha": "*",
    "better-assert": "*",
    "through": "^2.3.4",
    "stream-reduce": "^1.0.3",
    "from": "^0.1.3",
    "after": "^0.8.1"
  }
}
