{
  "name": "concurrent-map-stream",
  "description": "Concurrently transform a stream in FIFO order",
  "keywords": ["stream", "concurrent", "map"],
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/jb55/concurrent-map-stream.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec"
  },
  "dependencies": {
    "SimpleQueue": "0.0.1",
    "through": "^2.3.4"
  },
  "devDependencies": {
    "mocha": "*",
    "better-assert": "*",
    "stream-reduce": "^1.0.3"
  }
}
